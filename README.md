Типы:
feat — новая функциональность
fix — исправление бага
docs — изменения в документации
style — форматирование, отсутствующие точки с запятой и т.д. (не влияет на код)
refactor — рефакторинг кода
test — добавление или исправление тестов
chore — обновление сборщика, настройки и т.д.

# Crowdfunding (SPA)

Современное одностраничное приложение (SPA) для поиска и управления земельными участками. Проект выполнен на стеке React + Redux Toolkit с использованием кастомной сборки Webpack.

## 🚀 Текущий функционал

### 1. Система авторизации (Auth)

- **Login / Registration**: Реализована единая форма входа и регистрации с переключением режимов.
- **Интеграция с API**: Используется `DummyJSON API` для аутентификации пользователей через мутации RTK Query.
- **Управление состоянием**: Данные пользователя и токен безопасности сохраняются в глобальном стейте Redux (slice: `auth`).
- **Защита данных**: Обработка ошибок входа (неверный пароль/логин) выводится непосредственно в интерфейс формы.

### 2. Динамическая главная страница (Conditional Rendering)

- Главная страница (`HomePage`) является адаптивной:
  - **Для неавторизованных пользователей**: Отображается лендинг-секция (Intro/Welcome), побуждающая к регистрации.
  - **Для авторизованных пользователей**: Открывается доступ к функционалу «Locations» (поиск и интерактивная карта).

### 3. Интерактивный Header

- Динамическая навигация, меняющаяся в зависимости от статуса `isAuth`.
- **Профиль пользователя**: Выпадающее меню с информацией о пользователе (имя, почта, аватар) и кнопкой Logout.
- **Корзина (Basket)**: Компонент с индикатором количества выбранных позиций (badge).
- **Адаптивность**: Реализовано боковое меню (Sidebar) для мобильных устройств.

---

## 🛠 Технологический стек

- **Core**: React 18
- **State Management**: Redux Toolkit
- **Data Fetching**: RTK Query
- **Routing**: React Router v6
- **Styling**: Native CSS Modules (без внешних библиотек)
- **Build Tool**: Custom Webpack Configuration

## 📦 Список зависимостей (основные)

- `react`, `react-dom`, `react-router-dom`
- `@reduxjs/toolkit`, `react-redux`
- `leaflet`, `react-leaflet` (для реализации карт)

---

## 🏃 Инструкция по запуску
